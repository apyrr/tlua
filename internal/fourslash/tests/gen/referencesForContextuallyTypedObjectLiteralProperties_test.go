// Code generated by convertFourslash; DO NOT EDIT.
// To modify this test, run "npm run makemanual referencesForContextuallyTypedObjectLiteralProperties"

package fourslash_test

import (
	"testing"

	"github.com/apyrr/tlua/internal/fourslash"
	"github.com/apyrr/tlua/internal/testutil"
)

func TestReferencesForContextuallyTypedObjectLiteralProperties(t *testing.T) {
	fourslash.SkipIfFailing(t)
	t.Parallel()
	defer testutil.RecoverAndFail(t, "Panic on fourslash test")
	const content = `interface IFoo { /*xy*/xy: number; }

// Assignment
local a1: IFoo = { xy = 0 };
local a2: IFoo = { xy = 0 };

// Function call
function consumer(f: IFoo) end
consumer({ xy = 1 });

// Type cast
local c = <IFoo>{ xy = 0 };

// Array literal
local ar: IFoo[] = [{ xy = 1 }, { xy = 2 }];

// Nested object literal
local ob: { ifoo: IFoo } = { ifoo = { xy = 0 } };

// Widened type
local w: IFoo = { xy = undefined };

// Untped -- should not be included
local u = { xy = 0 };`
	f, done := fourslash.NewFourslash(t, nil /*capabilities*/, content)
	defer done()
	f.VerifyBaselineFindAllReferences(t, "xy")
}
