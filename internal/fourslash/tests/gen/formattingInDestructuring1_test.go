// Code generated by convertFourslash; DO NOT EDIT.
// To modify this test, run "npm run makemanual formattingInDestructuring1"

package fourslash_test

import (
	"testing"

	"github.com/apyrr/tlua/internal/fourslash"
	"github.com/apyrr/tlua/internal/testutil"
)

func TestFormattingInDestructuring1(t *testing.T) {
	fourslash.SkipIfFailing(t)
	t.Parallel()
	defer testutil.RecoverAndFail(t, "Panic on fourslash test")
	const content = `interface local { }
/*1*/local x: local         [];

function foo() {
    'use strict'
/*2*/    local        [x] = [];
/*3*/    local      [x] = [];
/*4*/    for (let[x] = [];x < 1;) {
    }
}`
	f, done := fourslash.NewFourslash(t, nil /*capabilities*/, content)
	defer done()
	f.FormatDocument(t, "")
	f.GoToMarker(t, "1")
	f.VerifyCurrentLineContent(t, `local x: let[];`)
	f.GoToMarker(t, "2")
	f.VerifyCurrentLineContent(t, `    local [x] = [];`)
	f.GoToMarker(t, "3")
	f.VerifyCurrentLineContent(t, `    local [x] = [];`)
	f.GoToMarker(t, "4")
	f.VerifyCurrentLineContent(t, `    for (local [x] = []; x < 1;) {`)
}
