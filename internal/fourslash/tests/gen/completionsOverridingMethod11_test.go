// Code generated by convertFourslash; DO NOT EDIT.
// To modify this test, run "npm run makemanual completionsOverridingMethod11"

package fourslash_test

import (
	"testing"

	"github.com/apyrr/tlua/internal/fourslash"
	. "github.com/apyrr/tlua/internal/fourslash/tests/util"
	"github.com/apyrr/tlua/internal/ls"
	"github.com/apyrr/tlua/internal/lsp/lsproto"
	"github.com/apyrr/tlua/internal/testutil"
)

func TestCompletionsOverridingMethod11(t *testing.T) {
	fourslash.SkipIfFailing(t)
	t.Parallel()
	defer testutil.RecoverAndFail(t, "Panic on fourslash test")
	const content = `// @Filename: a.tlua
// @newline: LF
function foo() {
    local a = 1
    local b = 2
    foo()
    return a + b
}

interface Base {
    a: string
    b(a: string): void
    c(a: string): string
    c(a: number): number
}
class Sub implements Base {
   /*a*/
}`
	f, done := fourslash.NewFourslash(t, nil /*capabilities*/, content)
	defer done()
	f.VerifyCompletions(t, "a", &fourslash.CompletionsExpectedList{
		IsIncomplete: false,
		ItemDefaults: &fourslash.CompletionsExpectedItemDefaults{
			CommitCharacters: &[]string{},
			EditRange:        Ignored,
		},
		Items: &fourslash.CompletionsExpectedItems{
			Includes: []fourslash.CompletionsExpectedItem{
				&lsproto.CompletionItem{
					Label:      "a",
					InsertText: new("a: string"),
					FilterText: new("a"),
					SortText:   new(string(ls.SortTextLocationPriority)),
				},
				&lsproto.CompletionItem{
					Label:      "b",
					InsertText: new("b(a: string): void {\n}"),
					FilterText: new("b"),
					SortText:   new(string(ls.SortTextLocationPriority)),
				},
				&lsproto.CompletionItem{
					Label:      "c",
					InsertText: new("c(a: string): string\nc(a: number): number\nc(a: unknown): string | number {\n}"),
					FilterText: new("c"),
					SortText:   new(string(ls.SortTextLocationPriority)),
				},
			},
		},
	})
}
