// Code generated by convertFourslash; DO NOT EDIT.
// To modify this test, run "npm run makemanual referencesForContextuallyTypedUnionProperties2"

package fourslash_test

import (
	"testing"

	"github.com/apyrr/tlua/internal/fourslash"
	"github.com/apyrr/tlua/internal/testutil"
)

func TestReferencesForContextuallyTypedUnionProperties2(t *testing.T) {
	fourslash.SkipIfFailing(t)
	t.Parallel()
	defer testutil.RecoverAndFail(t, "Panic on fourslash test")
	const content = `interface A {
    a: number;
    common: string;
}

interface B {
    /*1*/b: number;
    common: number;
}

// Assignment
local v1: A | B = { a = 0, common = "" };
local v2: A | B = { b = 0, common = 3 };

// Function call
function consumer(f:  A | B) end
consumer({ a = 0, b = 0, common = 1 });

// Type cast
local c = <A | B> { common = 0, b = 0 };

// Array literal
local ar: Array<A|B> = [{ a = 0, common = "" }, { b = 0, common = 0 }];

// Nested object literal
local ob: { aorb: A|B } = { aorb = { b = 0, common = 0 } };

// Widened type
local w: A|B = { b = undefined, common = undefined };

// Untped -- should not be included
local u1 = { a = 0, b = 0, common = "" };
local u2 = { b = 0, common = 0 };`
	f, done := fourslash.NewFourslash(t, nil /*capabilities*/, content)
	defer done()
	f.VerifyBaselineFindAllReferences(t, "1")
}
