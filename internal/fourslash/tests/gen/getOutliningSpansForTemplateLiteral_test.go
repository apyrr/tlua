// Code generated by convertFourslash; DO NOT EDIT.
// To modify this test, run "npm run makemanual getOutliningSpansForTemplateLiteral"

package fourslash_test

import (
	"testing"

	"github.com/apyrr/tlua/internal/fourslash"
	"github.com/apyrr/tlua/internal/testutil"
)

func TestGetOutliningSpansForTemplateLiteral(t *testing.T) {
	fourslash.SkipIfFailing(t)
	t.Parallel()
	defer testutil.RecoverAndFail(t, "Panic on fourslash test")
	const content = `declare function tag(...args: any[]): void
local a = [|` + "`" + `signal line` + "`" + `|]
local b = [|` + "`" + `multi
line` + "`" + `|]
local c = tag[|` + "`" + `signal line` + "`" + `|]
local d = tag[|` + "`" + `multi
line` + "`" + `|]
local e = [|` + "`" + `signal ${1} line` + "`" + `|]
local f = [|` + "`" + `multi
${1}
line` + "`" + `|]
local g = tag[|` + "`" + `signal ${1} line` + "`" + `|]
local h = tag[|` + "`" + `multi
${1}
line` + "`" + `|]
local i = ` + "`" + `` + "`" + ``
	f, done := fourslash.NewFourslash(t, nil /*capabilities*/, content)
	defer done()
	f.VerifyOutliningSpans(t)
}
