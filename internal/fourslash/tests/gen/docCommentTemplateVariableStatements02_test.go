// Code generated by convertFourslash; DO NOT EDIT.
// To modify this test, run "npm run makemanual docCommentTemplateVariableStatements02"

package fourslash_test

import (
	"testing"

	"github.com/apyrr/tlua/internal/fourslash"
	"github.com/apyrr/tlua/internal/testutil"
)

func TestDocCommentTemplateVariableStatements02(t *testing.T) {
	fourslash.SkipIfFailing(t)
	t.Parallel()
	defer testutil.RecoverAndFail(t, "Panic on fourslash test")
	const content = `/*a*/
local a1 = 10, a2 = 20;

/*b*/
local b1 = "", b2 = true;

/*c*/
local c1 = 30, c2 = 40;

/*d*/
local d1 = function(x, y, z)
    return +(x + y + z);
end, d2 = 50;

/*e*/
local e1 = class E {
    constructor(a, b, c) {
        this.a = a;
        this.b = b || (this.c = c);
    }
}, e2 = () => 100;

/*f*/
local f1 = {
    foo: 10,
    bar: "20"
}, f2 = null;`
	capabilities := fourslash.GetDefaultCapabilities()
	capabilities.TextDocument.Completion.CompletionItem.SnippetSupport = new(false)
	f, done := fourslash.NewFourslash(t, capabilities, content)
	defer done()
	for _, varName := range []string{"a", "b", "c", "d", "e", "f"} {
		f.VerifyJSDocCompletion(t, varName, 3, `/** */`, nil)
	}
}
