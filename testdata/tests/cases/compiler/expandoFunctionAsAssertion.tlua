// @strict: true
// @noEmit: true

function example() end

example.isFoo = function(value: string): asserts value is 'foo'
  if value ~= 'foo' then
    error('Not foo');
  end
end;

example.isFoo('test');
