-- ported from tests/cases/compiler/noUnusedLocals_potentialPredicateUnusedParam.ts
-- dropped: @target: es2015 and @strict: true directives (tlua defaults to the latest target and to strict mode)
-- rewritten: the JS global `Math.random()` became the Lua-native `math.random()`; the double negation is preserved so the return stays a widened boolean

// @noEmit: true
// @noUnusedLocals: true
// @noUnusedParameters: true

function potentialPredicateUnusedParam(a: unknown)
    return not not math.random()
end
