-- ported from tests/cases/compiler/optionalParamReferencingOtherParams3.ts
// @noEmit: true
-- dropped: @target: es2015 and @strict: false directives (tlua uses latest target and strict checking)
-- dropped: bare expression statements rewritten as local bindings (bare expressions are invalid in tlua)
-- compiler gap: default parameter initializers emit invalid Lua parameter syntax; checker coverage is retained.
-- dropped: emit coverage suppressed because the generated function parameters are not valid Lua.

function right(a = b, b = a)
    local _ = a
    local _ = b
end
