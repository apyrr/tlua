early.tlua(4,1): error TLUA100053: A top-level 'return' must be the last statement of its block.
nestedInvalid.tlua(3,3): error TLUA100053: A top-level 'return' must be the last statement of its block.


==== early.tlua (1 errors) ====
    // A chunk returns its value as its last statement; an earlier return is not
    // the module value.
    local M = {};
    return M;
    ~~~~~~
!!! error TLUA100053: A top-level 'return' must be the last statement of its block.
    local after = 1;
    
==== nested.tlua (0 errors) ====
    declare flag: boolean;
    if flag then
      return 1;
    end
    return 2;
    
==== nestedInvalid.tlua (1 errors) ====
    declare flag: boolean;
    if flag then
      return 1;
      ~~~~~~
!!! error TLUA100053: A top-level 'return' must be the last statement of its block.
      local after = 2;
    end
    