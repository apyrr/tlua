//// [tests/cases/compiler/shorthandPropertyAssignmentNoCrash.tlua] ////

=== shorthandPropertyAssignmentNoCrash.tlua ===
// https://github.com/microsoft/typescript-go/issues/3789

function ff(f: any) {
>ff : (f: any) => void
>f : any

    local g;
>g : any

    ({ g = (x: any, y: any) => x + y } = f);
>({ g = (x: any, y: any) => x + y } = f) : any
>{ g = (x: any, y: any) => x + y } = f : any
>{ g = (x: any, y: any) => x + y } : { g: (x: any, y: any) => any; }
>g : (x: any, y: any) => any
>(x: any, y: any) => x + y : (x: any, y: any) => any
>x : any
>y : any
>x + y : any
>x : any
>y : any
>f : any
}

