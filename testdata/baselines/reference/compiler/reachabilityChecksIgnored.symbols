//// [tests/cases/compiler/reachabilityChecksIgnored.tlua] ////

=== reachabilityChecksIgnored.tlua ===
function a()
>a : Symbol(a, Decl(reachabilityChecksIgnored.tlua, 0, 0))

    throw "";

    // @ts-ignore
    console.log("unreachable");
end

function b()
>b : Symbol(b, Decl(reachabilityChecksIgnored.tlua, 5, 3))

    throw "";

    // @ts-expect-error
    console.log("unreachable");
end

