tluaSuspendAmbientDeclaration.tlua(9,3): error TLUA100023: A suspend function can only be called from a suspend context.
tluaSuspendAmbientDeclaration.tlua(25,7): error TLUA2322: Type 'suspend (url: string) => (boolean, any)' is not assignable to type '(url: string) => (boolean, any)'.
  A suspend function is not assignable to a non-suspend function type.
tluaSuspendAmbientDeclaration.tlua(28,9): error TLUA1029: 'declare' modifier must precede 'suspend' modifier.


==== tluaSuspendAmbientDeclaration.tlua (3 errors) ====
    // The ambient form of the suspend contract: `declare suspend function` is
    // what declaration files emit, so the checker must both accept it and keep
    // enforcing caller-context coloring through it.
    
    declare suspend function fetchOk(url: string): (boolean, any);
    
    // Error: direct call from a sync function.
    function syncCaller(): void
      fetchOk("nope");
      ~~~~~~~
!!! error TLUA100023: A suspend function can only be called from a suspend context.
!!! related TLUA100058 tluaSuspendAmbientDeclaration.tlua:8:10: Did you mean to mark this function as 'suspend'?
    end
    
    // OK: call from a suspend context.
    suspend function suspendCaller(): number
      local ok, res = fetchOk("https://example.com");
      if ok then
        return 1;
      end
      return 0;
    end
    
    // OK: passing the ambient suspend function as a value is not a call.
    coroutine.create(fetchOk);
    
    // Error: a concrete sync signature still rejects the ambient suspend function.
    local concrete: (url: string) => (boolean, any) = fetchOk;
          ~~~~~~~~
!!! error TLUA2322: Type 'suspend (url: string) => (boolean, any)' is not assignable to type '(url: string) => (boolean, any)'.
!!! error TLUA2322:   A suspend function is not assignable to a non-suspend function type.
    
    // Error: `declare` must precede `suspend`.
    suspend declare function backwards(): void;
            ~~~~~~~
!!! error TLUA1029: 'declare' modifier must precede 'suspend' modifier.
    