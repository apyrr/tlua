//// [tests/cases/compiler/tluaTableAugmentationDefaultedGuardCrossFile.tlua] ////

=== roots.tlua ===
Forward = Forward or {};
>Forward = Forward or {} : { zod: { value: boolean; }; }
>Forward : { zod: { value: boolean; }; }
>Forward or {} : { zod: { value: boolean; }; }
>Forward : { zod: { value: boolean; }; }
>{} : { zod: { value: boolean; }; }

MixedKey = MixedKey or {};
>MixedKey = MixedKey or {} : { zod: { value: number; }; }
>MixedKey : { zod: { value: number; }; }
>MixedKey or {} : { zod: { value: number; }; }
>MixedKey : { zod: { value: number; }; }
>{} : { zod: { value: number; }; }

Parenthesized = Parenthesized or {};
>Parenthesized = Parenthesized or {} : { zod: any; }
>Parenthesized : { zod: any; }
>Parenthesized or {} : { zod: any; }
>Parenthesized : { zod: any; }
>{} : { zod: any; }

=== members.tlua ===
Forward.zod = Forward.zod or {};
>Forward.zod = Forward.zod or {} : { value: boolean; }
>Forward.zod : { value: boolean; }
>Forward : { zod: { value: boolean; }; }
>zod : { value: boolean; }
>Forward.zod or {} : { value: boolean; }
>Forward.zod : { value: boolean; }
>Forward : { zod: { value: boolean; }; }
>zod : { value: boolean; }
>{} : { value: boolean; }

Forward.zod.value = true;
>Forward.zod.value = true : true
>Forward.zod.value : boolean
>Forward.zod : { value: boolean; }
>Forward : { zod: { value: boolean; }; }
>zod : { value: boolean; }
>value : boolean
>true : true

MixedKey.zod = MixedKey["zod"] or {};
>MixedKey.zod = MixedKey["zod"] or {} : { value: number; }
>MixedKey.zod : { value: number; }
>MixedKey : { zod: { value: number; }; }
>zod : { value: number; }
>MixedKey["zod"] or {} : { value: number; }
>MixedKey["zod"] : { value: number; }
>MixedKey : { zod: { value: number; }; }
>"zod" : "zod"
>{} : { value: number; }

MixedKey.zod.value = 1;
>MixedKey.zod.value = 1 : 1
>MixedKey.zod.value : number
>MixedKey.zod : { value: number; }
>MixedKey : { zod: { value: number; }; }
>zod : { value: number; }
>value : number
>1 : 1

// Parenthesized self-reads are not the exact defaulted-guard idiom.
Parenthesized.zod = (Parenthesized.zod) or {};
>Parenthesized.zod = (Parenthesized.zod) or {} : any
>Parenthesized.zod : any
>Parenthesized : { zod: any; }
>zod : any
>(Parenthesized.zod) or {} : any
>(Parenthesized.zod) : any
>Parenthesized.zod : any
>Parenthesized : { zod: any; }
>zod : any
>{} : {}

=== reverseMember.tlua ===
// Root resolution is program-wide, not source-file-order dependent.
Reverse.zod = Reverse.zod or {};
>Reverse.zod = Reverse.zod or {} : { value: string; }
>Reverse.zod : { value: string; }
>Reverse : { zod: { value: string; }; }
>zod : { value: string; }
>Reverse.zod or {} : { value: string; }
>Reverse.zod : { value: string; }
>Reverse : { zod: { value: string; }; }
>zod : { value: string; }
>{} : { value: string; }

Reverse.zod.value = "reverse";
>Reverse.zod.value = "reverse" : "reverse"
>Reverse.zod.value : string
>Reverse.zod : { value: string; }
>Reverse : { zod: { value: string; }; }
>zod : { value: string; }
>value : string
>"reverse" : "reverse"

=== reverseRoot.tlua ===
Reverse = Reverse or {};
>Reverse = Reverse or {} : { zod: { value: string; }; }
>Reverse : { zod: { value: string; }; }
>Reverse or {} : { zod: { value: string; }; }
>Reverse : { zod: { value: string; }; }
>{} : { zod: { value: string; }; }

=== reads.tlua ===
local forward: boolean = Forward.zod.value;
>forward : boolean
>Forward.zod.value : boolean
>Forward.zod : { value: boolean; }
>Forward : { zod: { value: boolean; }; }
>zod : { value: boolean; }
>value : boolean

local mixedKey: number = MixedKey.zod.value;
>mixedKey : number
>MixedKey.zod.value : number
>MixedKey.zod : { value: number; }
>MixedKey : { zod: { value: number; }; }
>zod : { value: number; }
>value : number

local reverse: string = Reverse.zod.value;
>reverse : string
>Reverse.zod.value : string
>Reverse.zod : { value: string; }
>Reverse : { zod: { value: string; }; }
>zod : { value: string; }
>value : string

