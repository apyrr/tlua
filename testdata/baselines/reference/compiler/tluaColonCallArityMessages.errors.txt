tluaColonCallArityMessages.tlua(10,12): error TLUA2554: Expected 0 arguments, but got 1.
tluaColonCallArityMessages.tlua(11,10): error TLUA2554: Expected 0 arguments, but got 2.
tluaColonCallArityMessages.tlua(14,6): error TLUA2554: Expected 1-2 arguments, but got 0.
tluaColonCallArityMessages.tlua(18,6): error TLUA2554: Expected 1 arguments, but got 0.
tluaColonCallArityMessages.tlua(19,6): error TLUA2554: Expected 2-3 arguments, but got 1.
tluaColonCallArityMessages.tlua(23,8): error TLUA2554: Expected 2-3 arguments, but got 1.
tluaColonCallArityMessages.tlua(27,8): error TLUA2555: Expected at least 1 arguments, but got 0.
tluaColonCallArityMessages.tlua(28,8): error TLUA2555: Expected at least 2 arguments, but got 1.
tluaColonCallArityMessages.tlua(32,1): error TLUA2554: Expected 1 arguments, but got 2.
tluaColonCallArityMessages.tlua(33,1): error TLUA2554: Expected 1 arguments, but got 0.
tluaColonCallArityMessages.tlua(34,1): error TLUA2554: Expected 2 arguments, but got 3.
tluaColonCallArityMessages.tlua(45,6): error TLUA2575: No overload expects 2 arguments, but overloads do exist that expect either 0 or 3 arguments.
tluaColonCallArityMessages.tlua(53,19): error TLUA2554: Expected 0 arguments, but got 3.
tluaColonCallArityMessages.tlua(59,33): error TLUA100037: Expected at most 2 arguments, but a tail may pass more.


==== tluaColonCallArityMessages.tlua (14 errors) ====
    // A colon call passes its receiver as the implicit first argument, so the
    // effective argument list the checker matches against the signature is one
    // longer than the source. An arity message counts what was WRITTEN, or it
    // describes an argument list the reader cannot see: `text:upper(1)` takes no
    // written arguments at all, so "expected 1, got 2" names neither the slot the
    // user can fill nor the argument they typed.
    
    local text: string = "hello";
    
    text:upper(1);
               ~
!!! error TLUA2554: Expected 0 arguments, but got 1.
    text:len(1, 2);
             ~~~~
!!! error TLUA2554: Expected 0 arguments, but got 2.
    
    // Too few, measured the same way: `sub` takes `self` plus a start index.
    text:sub();
         ~~~
!!! error TLUA2554: Expected 1-2 arguments, but got 0.
!!! related TLUA6210 lib.luajit.d.tlua:249:23: An argument for 'i' was not provided.
    
    // The dot form writes its receiver, so counting it is correct there -- these
    // messages must NOT shift.
    text.upper();
         ~~~~~
!!! error TLUA2554: Expected 1 arguments, but got 0.
!!! related TLUA6210 lib.luajit.d.tlua:250:11: An argument for 'self' was not provided.
    text.sub(text);
         ~~~
!!! error TLUA2554: Expected 2-3 arguments, but got 1.
!!! related TLUA6210 lib.luajit.d.tlua:249:23: An argument for 'i' was not provided.
    
    // The namespace form never had a receiver to hide.
    local nsPiece: string = string.sub(text, 1, 5);
    string.sub(text);
           ~~~
!!! error TLUA2554: Expected 2-3 arguments, but got 1.
!!! related TLUA6210 lib.luajit.d.tlua:266:24: An argument for 'i' was not provided.
    
    declare buffer: LuaStringBuffer;
    // A rest parameter reports a minimum, and the receiver comes out of that too.
    buffer:put();
           ~~~
!!! error TLUA2555: Expected at least 1 arguments, but got 0.
!!! related TLUA6210 lib.luajit.d.tlua:589:32: An argument for 'value' was not provided.
    buffer.put(buffer);
           ~~~
!!! error TLUA2555: Expected at least 2 arguments, but got 1.
!!! related TLUA6210 lib.luajit.d.tlua:589:32: An argument for 'value' was not provided.
    
    // string.format checks arity against the format string's specifiers, on its
    // own path -- the receiver has to come out of both counts there as well.
    ("%d"):format(1, 2);
    ~~~~~~~~~~~~~~~~~~~
!!! error TLUA2554: Expected 1 arguments, but got 2.
    ("%d"):format();
    ~~~~~~~~~~~~~~~
!!! error TLUA2554: Expected 1 arguments, but got 0.
    string.format("%d", 1, 2);
    ~~~~~~~~~~~~~~~~~~~~~~~~~
!!! error TLUA2554: Expected 2 arguments, but got 3.
    
    // The overload branch: no candidate matches, so the message lists the counts the
    // candidates DO accept. Those are written counts too, and a candidate with no
    // parameter for the receiver to fill cannot be described that way at all -- the
    // message then reports the mismatch in the terms it happened in rather than
    // collapsing distinct counts into "no overload expects 0 ... expects either 0".
    interface String {
      overloaded(): string;
      overloaded(self: string, a: string, b: string): string;
    }
    text:overloaded("only-one");
         ~~~~~~~~~~
!!! error TLUA2575: No overload expects 2 arguments, but overloads do exist that expect either 0 or 3 arguments.
    
    // A receiver-filled optional parameter genuinely requires nothing written, so a
    // clamped minimum of zero is the truth rather than a lost negative.
    interface LuaFile {
      rewind(whence?: string): number;
    }
    declare rewindable: LuaFile;
    rewindable:rewind("set", 1, 2);
                      ~~~~~~~~~~~
!!! error TLUA2554: Expected 0 arguments, but got 3.
    
    // The pack-tail branch: every parameter is already filled, so a `...` in final
    // position can only supply values Lua would discard -- and the parameter the
    // receiver filled is not part of the count the reader sees ("at most 2", not 3).
    function spreadIntoColonCall(...: string)
      local spread = text:sub(1, 2, ...);
                                    ~~~
!!! error TLUA100037: Expected at most 2 arguments, but a tail may pass more.
    end
    