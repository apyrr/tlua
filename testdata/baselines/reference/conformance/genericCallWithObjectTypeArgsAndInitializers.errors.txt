genericCallWithObjectTypeArgsAndInitializers.tlua(10,17): error TLUA2322: Type 'nil' is not assignable to type 'T'.
  'T' could be instantiated with an arbitrary type which could be unrelated to 'nil'.
genericCallWithObjectTypeArgsAndInitializers.tlua(14,33): error TLUA2322: Type 'number' is not assignable to type 'T'.
  'number' is assignable to the constraint of type 'T', but 'T' could be instantiated with a different subtype of constraint 'number'.
genericCallWithObjectTypeArgsAndInitializers.tlua(17,37): error TLUA2322: Type 'T' is not assignable to type 'U'.
  'U' could be instantiated with an arbitrary type which could be unrelated to 'T'.
genericCallWithObjectTypeArgsAndInitializers.tlua(23,56): error TLUA2322: Type 'U' is not assignable to type 'V'.
  'V' could be instantiated with an arbitrary type which could be unrelated to 'U'.


==== genericCallWithObjectTypeArgsAndInitializers.tlua (4 errors) ====
    -- ported from tests/cases/conformance/types/typeRelationships/assignmentCompatibility/genericCallWithObjectTypeArgsAndInitializers.ts
    -- dropped: @target: es2015 directive (tlua defaults to the latest target)
    -- note: upstream's `T extends Number` wrapper constraint is written as tlua's primitive
    -- `number` constraint, which keeps the subject: the initializer satisfies the constraint
    -- but is still unsafe for an arbitrary subtype of it.
    -- dropped: upstream's separate `foo2<T>(x: T = undefined)` case -- tlua has a single nil
    -- value, so that case is textually identical to the `x: T = nil` case above it.
    
    -- Generic typed parameters with initializers
    function foo<T>(x: T = nil)
                    ~~~~~~~~~~
!!! error TLUA2322: Type 'nil' is not assignable to type 'T'.
!!! error TLUA2322:   'T' could be instantiated with an arbitrary type which could be unrelated to 'nil'.
        return x
    end -- error
    
    function foo3<T extends number>(x: T = 1)
                                    ~~~~~~~~
!!! error TLUA2322: Type 'number' is not assignable to type 'T'.
!!! error TLUA2322:   'number' is assignable to the constraint of type 'T', but 'T' could be instantiated with a different subtype of constraint 'number'.
    end -- error
    
    function foo4<T, U extends T>(x: T, y: U = x)
                                        ~~~~~~~~
!!! error TLUA2322: Type 'T' is not assignable to type 'U'.
!!! error TLUA2322:   'U' could be instantiated with an arbitrary type which could be unrelated to 'T'.
!!! related TLUA2208 genericCallWithObjectTypeArgsAndInitializers.tlua:17:15: This type parameter might need an `extends U` constraint.
    end -- error
    
    function foo5<T, U extends T>(x: U, y: T = x)
    end -- ok
    
    function foo6<T, U extends T, V extends U>(x: T, y: U, z: V = y)
                                                           ~~~~~~~~
!!! error TLUA2322: Type 'U' is not assignable to type 'V'.
!!! error TLUA2322:   'V' could be instantiated with an arbitrary type which could be unrelated to 'U'.
    end -- error
    
    function foo7<T, U extends T, V extends U>(x: V, y: U = x)
    end -- should be ok
    