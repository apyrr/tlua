parserErrorRecovery_ParameterList1.tlua(9,1): error TLUA1005: ',' expected.
parserErrorRecovery_ParameterList1.tlua(10,1): error TLUA1005: ')' expected.


==== parserErrorRecovery_ParameterList1.tlua (2 errors) ====
    -- ported from tests/cases/conformance/parser/ecmascript5/ErrorRecovery/ParameterLists/parserErrorRecovery_ParameterList1.ts
    -- dropped: @target: es2015 (tlua targets latest; the ES target is not a tlua concept)
    -- rewritten: tlua has no `{`-delimited function body (the body starts after `)` and closes at
    -- `end`), so the malformed parameter list runs straight into `end`, which is the same recovery point.
    -- dropped: emit coverage (noEmit) -- the source is intentionally malformed, so whatever the parser
    -- recovers is not meaningful Lua to baseline.
    
    function f(a
    end
    ~~~
!!! error TLUA1005: ',' expected.
    
    
!!! error TLUA1005: ')' expected.