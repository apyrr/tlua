//// [tests/cases/conformance/ported/genericCallWithoutArgs.tlua] ////

=== genericCallWithoutArgs.tlua ===
-- ported from tests/cases/compiler/genericCallWithoutArgs.ts
-- dropped: @target: es2015 directive (tlua defaults to latest target)
-- dropped: emit coverage (noEmit) — malformed member-access recovery has no valid Lua lowering


function f<X, Y>(x: X, y: Y)
>f : <X, Y>(x: X, y: Y) => void
>x : X
>y : Y

end

f<number, string>.
>f<number, string>. : any
>f<number, string> : (x: number, y: string) => void
>f : <X, Y>(x: X, y: Y) => void

> : any

