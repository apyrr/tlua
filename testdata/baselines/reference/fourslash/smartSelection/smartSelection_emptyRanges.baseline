// === Smart Selection ===
class HomePage {
  componentDidMount(/**/) {
    if (this.props.username) {
      return '';
    }
  }
}

  componentDidMount()  ↲

  componentDidMount() {
    if (this.props.username) {
      return '';
    }
  }↲

class HomePage {
  componentDidMount() {
    if (this.props.username) {
      return '';
    }
  }
}

================================================================================

class HomePage {
  componentDidMount() {
    if (this.props.username/**/) {
      return '';
    }
  }
}


             props.username) {
      return '';
    }↲

    if (this.props.username) {
      return '';
    }↲

  componentDidMount() {
    if (this.props.username) {
      return '';
    }
  }↲

class HomePage {
  componentDidMount() {
    if (this.props.username) {
      return '';
    }
  }
}

================================================================================

class HomePage {
  componentDidMount() {
    if (this.props.username) {
      return '/**/';
    }
  }
}

             '' ↲
      return '';↲

             props.username) {
      return '';
    }↲

    if (this.props.username) {
      return '';
    }↲

  componentDidMount() {
    if (this.props.username) {
      return '';
    }
  }↲

class HomePage {
  componentDidMount() {
    if (this.props.username) {
      return '';
    }
  }
}